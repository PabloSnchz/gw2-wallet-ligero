function onAddKeyClick() {\n    const formHtml = `\n        <div id=\"apiKeyForm\">\n            <input type=\"text\" id=\"apiKeyName\" placeholder=\"Enter name\" required>\n            <input type=\"text\" id=\"apiKey\" placeholder=\"Enter API key\" required>\n            <button id=\"submitApiKey\">Add API Key</button>\n        </div>\n    `;\n    \n    const apiKeyContainer = document.getElementById('apiKeyContainer');\n    apiKeyContainer.innerHTML += formHtml;\n    \n    document.getElementById('submitApiKey').onclick = function() {\n        const name = document.getElementById('apiKeyName').value;\n        const key = document.getElementById('apiKey').value;\n        \n        if (validateKey(name, key)) {\n            localStorage.setItem(name, key);\n            displayKeys();\n            selectKey(name);\n        }\n    };\n}\n\nfunction validateKey(name, key) {\n    // Add validation logic here\n    return name && key; // Example validation\n}\n\nfunction displayKeys() {\n    // Logic to display keys\n}\n\nfunction selectKey(name) {\n    // Logic to auto-select the key\n}